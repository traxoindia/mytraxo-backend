server:
  port: ${SERVER_PORT:8080}

spring:
  data:
    mongodb:
      uri: ${MONGO_URI}

app:
  jwt:
    secret: ${JWT_SECRET}
    accessMinutes: 15
    refreshDays: 30

app:
  cors:
    allowedOrigins: ${CORS_ALLOWED_ORIGINS}